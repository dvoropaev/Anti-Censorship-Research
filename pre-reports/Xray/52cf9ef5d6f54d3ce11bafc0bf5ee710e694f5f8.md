# Commit 52cf9ef5d6f54d3ce11bafc0bf5ee710e694f5f8

## commit message
- **TUN inbound: Better `"autoOutboundsInterface": "auto"` (#6035)**
- Дата в changelog: `2026-05-02 20:18:25`.
- https://github.com/XTLS/Xray-core/pull/6035#issuecomment-4336755860
- Fixes https://github.com/XTLS/Xray-core/issues/6030

## diff
```diff
diff --git a/proxy/tun/config.go b/proxy/tun/config.go
index cef14b53..cdeeeffa 100644
--- a/proxy/tun/config.go
+++ b/proxy/tun/config.go
@@ -3,6 +3,8 @@ package tun
 import (
 	"context"
 	"net"
+	"sort"
+	"strings"
 	"sync"
 
 	"github.com/xtls/xray-core/common/errors"
@@ -45,26 +47,53 @@ func (updater *InterfaceUpdater) Update() {
 	}
 
 	var got *net.Interface
-	for _, iface := range interfaces {
-		if iface.Index == updater.tunIndex {
-			continue
-		}
-		if updater.fixedName != "" {
+	if updater.fixedName != "" {
+		for _, iface := range interfaces {
+			if iface.Index == updater.tunIndex {
+				continue
+			}
 			if iface.Name == updater.fixedName {
 				got = &iface
 				break
 			}
-		} else {
+		}
+	} else {
+		var ifs []struct {
+			index int
+			score int
+		}
+		for i, iface := range interfaces {
+			if iface.Index == updater.tunIndex {
+				continue
+			}
+			if strings.Contains(iface.Name, "vEthernet") {
+				continue
+			}
+			if iface.Flags&net.FlagUp == 0 {
+				continue
+			}
+			if iface.Flags&net.FlagLoopback != 0 {
+				continue
+			}
 			addrs, err := iface.Addrs()
-			if err != nil {
+			if err != nil || len(addrs) == 0 {
 				continue
 			}
-			if (iface.Flags&net.FlagUp != 0) &&
-				(iface.Flags&net.FlagLoopback == 0) &&
-				len(addrs) > 0 {
-				got = &iface
-				break
+			ifs = append(ifs, struct {
+				index int
+				score int
+			}{i, score(&iface, addrs)})
+		}
+		sort.Slice(ifs, func(i, j int) bool {
+			if ifs[i].score != ifs[j].score {
+				return ifs[i].score > ifs[j].score
 			}
+
+			return interfaces[ifs[i].index].Name < interfaces[ifs[j].index].Name
+		})
+		if len(ifs) > 0 {
+			iface := interfaces[ifs[0].index]
+			got = &iface
 		}
 	}
 
@@ -76,3 +105,21 @@ func (updater *InterfaceUpdater) Update() {
 	updater.iface = got
 	errors.LogInfo(context.Background(), "[tun] update interface ", got.Name, " ", got.Index)
 }
+
+func score(iface *net.Interface, addrs []net.Addr) int {
+	score := 0
+
+	name := strings.ToLower(iface.Name)
+	if strings.Contains(name, "wlan") || strings.Contains(name, "wi-fi") {
+		score += 2
+	}
+
+	for _, addr := range addrs {
+		if strings.HasPrefix(addr.String(), "192.168.") {
+			score += 1
+			break
+		}
+	}
+
+	return score
+}
diff --git a/proxy/tun/tun_windows.go b/proxy/tun/tun_windows.go
index c8c00d4e..3072b612 100644
--- a/proxy/tun/tun_windows.go
+++ b/proxy/tun/tun_windows.go
@@ -177,9 +177,6 @@ func (t *WindowsTun) Start() error {
 
 	if updater != nil {
 		t.changeCallback, err = winipcfg.RegisterInterfaceChangeCallback(func(notificationType winipcfg.MibNotificationType, iface *winipcfg.MibIPInterfaceRow) {
-			if notificationType != winipcfg.MibDeleteInstance {
-				return
-			}
 			updater.Update()
 		})
 		if err != nil {

```

## список изменённых/добавленных/удаленных сущностей и их описание
- `proxy/tun/config.go` — изменённый файл, через него реализовано изменение из коммита.
- `proxy/tun/tun_windows.go` — изменённый файл, через него реализовано изменение из коммита.

## подробный разбор сущностей и взаимодействий
Изменение затрагивает конфигурационные и/или кодовые сущности, перечисленные выше. Для каждой сущности важно следующее: она получает входные данные либо из JSON/CLI-конфига, либо из сетевого стека проекта, затем передаёт их в слой обработки (парсер, роутер, inbound/outbound, транспорт или CI-обвязку — в зависимости от файла). До изменения поведение определялось предыдущей версией логики в этих же файлах; после изменения добавлены новые ветки обработки, дополнительные проверки или обновлённые значения по умолчанию, что меняет итоговую реакцию системы на те же входные параметры.

Практически это выглядит так: вход (настройка или пакет) попадает в изменённый модуль, модуль применяет новые условия из коммита, после чего результат передаётся дальше по стандартному стеку проекта. Если это инфраструктурный коммит (README/CI), то изменяется процесс сопровождения: новые инструкции/проверки влияют на то, какие конфиги и версии считаются корректными.

До изменения оператору приходилось обходить ограничение вручную (например, через внешние настройки или доп.правила), а после изменения логика встроена в проектный код/конфиг напрямую. Если в commit message есть указание на bugfix, исправляется аварийный или некорректный сценарий (panic, неправильный маршрут, некорректный парсинг параметров, нестабильная работа клиента/транспорта).

## вопросы для читателя
1. Как изменение в `proxy/tun/config.go` влияет на совместимость с текущими конфигами?
2. Какие регрессионные тесты стоит добавить для проверки этого коммита?
3. Какие сценарии эксплуатации (клиент/сервер) наиболее чувствительны к этому изменению?
4. Как изменение взаимодействует с соседними подсистемами проекта?
5. Есть ли риски для производительности или потребления памяти?
6. Нужно ли обновлять документацию и примеры конфигурации?
